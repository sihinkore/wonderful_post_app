<h1>記事一覧</h1>

<div id="articles">
  <% @articles.each do |article| %>
    <%= render article %>
    <p>
     <%= link_to "記事の詳細", article %>
     <%= link_to "記事の編集", edit_article_path(article) %>
    </p>
  <% end %>
</div>

<%= link_to "新しい記事の作成", new_article_path %>
